//make a parent class component (App) that has two functional component children (gallery) and (form).
//The state and handlers should be stored in App.
//Allow the user to add a photo link. 
//Display the image (in the gallery) when the user clicks on the submit button. 
